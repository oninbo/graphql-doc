import React from 'react';
// NavLink

const Header = (props) => {

  return (
    <header className="app-header">
      <h1 className="title" >
        MPI Graphql-API Docs
      </h1>

    </header>
  )

}

export default Header;
